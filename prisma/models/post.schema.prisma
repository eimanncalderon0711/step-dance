model Post {
  id        Int     @id @default(autoincrement())
  title     String  @map("post_title")
  content   String  @default("") @map("post_content")
  published Boolean @default(false)

  authorId  Int?
  author    User? @relation(fields: [authorId], references: [id])

  @@map("posts")
}